# Auditoria — Notebook 02 (Evolução Temporal)

## 1. O que este notebook ensina?
Análise da evolução do consumo ao longo do tempo, identificando tendências e sazonalidade.

## 2. Código que estreou

**Datas**
- `pd.to_datetime()`
- `.dt.year`
- `.dt.month`

**Agregações temporais**
- `groupby()`
- `resample()` *(se utilizado)*

**Visualização**
- gráficos de linha (`plt.plot()`)

---

## 3. Código reaproveitado

**Leitura dos dados**
- `pd.read_csv()`

**Limpeza**
- `dropna()`
- `rename()`

**Agregações**
- `groupby()`
- `sort_values()`

**Configuração gráfica**
- `figsize`
- `title`
- `xlabel`
- `ylabel`
- `grid`

---

## 4. O que já poderia virar função?

- `carregar_dados()`
- `limpar_dados()`
- `configurar_grafico()`
- `preparar_datas()`

---

## 5. O que aprendi como analista?

Os dados também contam uma história no tempo. A análise temporal revelou tendências e padrões sazonais que não aparecem em análises estáticas.