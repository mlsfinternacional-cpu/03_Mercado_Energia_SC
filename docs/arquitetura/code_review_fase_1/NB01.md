# Auditoria — Notebook 01 (Exploração)

## 1. O que este notebook ensina?
Conhecimento da base de dados, estrutura, qualidade e exploração inicial.

## 2. Código que estreou
**Leitura dos dados**
- `pd.read_csv()`

**Inspeção da base**
- `head()`
- `info()`
- `shape`
- `columns`
- `dtypes`

**Qualidade dos dados**
- `isna()`
- `duplicated()`

**Transformações**
- seleção de colunas
- filtros
- criação de variáveis

**Agregações**
- `groupby()`
- `agg()`
- `sort_values()`

**Visualização**
- `matplotlib.pyplot`
- primeiros gráficos exploratórios

---

## 3. Código reaproveitado
Nenhum. Este notebook estabelece os padrões do projeto.

---

## 4. Candidatos a funções (`src`)
- leitura da base
- limpeza inicial
- configuração padrão dos gráficos

---

## 5. Aprendizado
A exploração dos dados antecede qualquer análise. Este notebook definiu a base metodológica para toda a pesquisa.