# Auditoria — Notebook 03 (Distribuição Regional)

## 1. O que este notebook ensina?
Analisa a distribuição regional do consumo de energia, comparando municípios e regiões para identificar padrões espaciais.

## 2. Código que estreou

**Integração de dados**
- `merge()`

**Organização**
- `sort_values()`

**Exploração regional**
- `unique()`
- `tolist()`

---

## 3. Código reaproveitado

**Leitura dos dados**
- `read_excel()`

**Limpeza**
- `drop_duplicates()`

**Inspeção**
- `head()`
- `info()`

**Manipulação de texto**
- `str.upper()`

---

## 4. O que já poderia virar função?

- `carregar_dados()`
- `padronizar_municipios()`
- `integrar_bases()`
- `configurar_grafico()`

---

## 5. O que aprendi como analista?

Análises regionais dependem de bases padronizadas. A integração e a consistência dos dados são fundamentais para comparar territórios com confiança.