# Auditoria — Notebook 04 (Distribuição das UC)

## 1. O que este notebook ensina?
Analisa a distribuição das Unidades Consumidoras (UC), organizando e agregando os dados para compreender como elas estão distribuídas entre municípios e regiões.

## 2. Código que estreou

**Agregação dos dados**
- `groupby()`
- `sum()`
- `mean()`

**Organização**
- `reset_index()`
- `rename()`
- `set_index()`

**Visualização**
- `plot.bar()`
- `plot.pie()`

---

## 3. Código reaproveitado

**Leitura dos dados**
- `read_excel()`

**Inspeção**
- `head()`
- `info()`

**Integração**
- `merge()`

**Organização**
- `sort_values()`

**Manipulação de texto**
- `str.upper()`

**Exploração**
- `tolist()`

---

## 4. O que já poderia virar função?

- `carregar_dados()`
- `padronizar_municipios()`
- `integrar_bases()`
- `agrupar_uc()`
- `configurar_grafico()`

---

## 5. O que aprendi como analista?

A distribuição das Unidades Consumidoras fica muito mais clara quando os dados são agregados antes da visualização. O processo reforça que a etapa de preparação dos dados é tão importante quanto o gráfico final, pois é ela que transforma registros individuais em informações úteis para análise territorial.